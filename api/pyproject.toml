[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
    "ASYNC",# flake8-async
]
ignore = [
    "B008",    # Depends() in defaults is the standard FastAPI pattern
    "ASYNC109",# timeout params in async functions are fine (asyncio.wait_for)
    "ASYNC240",# pathlib in async routes is acceptable for fast metadata ops
]

[tool.ruff.lint.isort]
known-first-party = ["app"]
